"""
Initialization module for the `include` package.

This module marks the `include` directory as a package, allowing test modules
to be structured and imported properly. It provides a namespace for different
ETL components, extractors, and utilities.

Structure
---------
The `include` package consists of the following submodules:

- `etl/`: Contains extract, transform, and load (ETL) components.
- `extractors/`: Modules responsible for data extraction from various sources.
- `utils/`: Utility functions for AWS, Azure, and file operations.
"""
