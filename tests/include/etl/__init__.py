"""
Initialization module for the `etl` package.

This module marks the `etl` directory as a package, organizing components for
data extraction, transformation, and loading (ETL). The modules within this
package facilitate structured data processing from raw ingestion to refined
storage.

Structure
---------
The `etl` package consists of:

- `coingecko/`: ETL components specific to CoinGecko data processing.
"""
